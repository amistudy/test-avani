<section class"custom-section">
    {% content_for 'blocks' %}
</section>

{% schema %}
    {
        "name" : "Sidekick section",
        "settings": [],
        "blocks" : [
            {
                "type": "@theme"
            },
            {
                "type": "@app"
            }
        ],
        "presets": [
            {
                "name": "Sidekick section",
                "category": "custom"
            }
        ]

    }

{% endschema %}